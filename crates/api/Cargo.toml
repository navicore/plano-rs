[package]
name = "plano-api"
version = "0.1.0"
edition = "2024"

[dependencies]
tonic = { workspace = true }
prost = { workspace = true }
tokio = { workspace = true }

[build-dependencies]
tonic-build = "0.13"

[package.metadata.build]
proto_root = "../../proto"
